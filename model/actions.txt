Edit Range=Edit the ranges of {columns}.
Drop Column=Remove columns that are irrelevant or redundant for the model.
Replace-Mean=Replace missing values in columns using the mean.
Replace-Median=Replace missing values in columns using the median.
Remove-Missing=Remove rows that contain missing values in columns.
Replace-Mode=Replace missing values in columns using the most frequent value (mode).
PCA=Apply Principal Component Analysis to reduce dimensionality.
outlier=Detect and handle outliers in columns.
AssignTarget=Assign columns as the target variables for model training.
ConvertToBoolean=Convert 1 and 0 values in columns to boolean (True/False).
Standardize=Standardize columns to have zero mean and unit variance.
Normalize=Normalize columns to scale values between 0 and 1.
Unbalancedness Upsample=Handle class imbalance by upsampling the minority class.
Unbalancedness DownSample=Handle class imbalance by downsampling the majority class.
Split=Split dataset into training and testing sets.
LabelEncoding=Convert categorical columns into numeric codes.
OneHotEncoding=Convert categorical columns into one-hot encoded vectors.
DataSet=Load a dataset.
ParameterFinetuning=Optimize model performance by modifying the parameters of the model.
ModelPerformance=Train a model and evaluate its performance.